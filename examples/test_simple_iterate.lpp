// Simple test of iterate-while operators

fn main() -> int {
    print("Testing auto-iterate operators");
    
    // Auto-increment
    let up = 0 !!< 5;
    print(up);
    
    // Auto-decrement
    let down = 10 !!> 5;
    print(down);
    
    return 0;
}
