// Test iterate-step operator ~>

fn main() -> int {
    print("Testing iterate-step operator");
    
    // Alternative syntax: start ~> step !! condition
    let powers = 1 ~> (x -> x * 2) !! (x -> x < 100);
    print(powers);
    
    return 0;
}
