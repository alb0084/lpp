#pragma paradigm functional

// This should FAIL - classes not allowed in functional paradigm

class BadClass {
    fn method() -> int {
        return 42;
    }
}

fn main() -> int {
    return 0;
}
