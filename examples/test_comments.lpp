#pragma paradigm hybrid

// Commento singola linea standard

/* Commento multilinea su singola riga */

/*
 * Commento multilinea
 * formattato su più righe
 * con asterischi
 */

fn calculate(x: int, y: int) -> int {
    // Calcola la somma
    let sum: int = x + y; /* inline comment */
    
    /* Commento
       su più righe
       nel corpo della funzione */
    
    return sum; // Ritorna il risultato
}

fn main() -> int {
    /*********************************
     * Header-style comment block    *
     *********************************/
    
    let result: int = calculate(10, 20); // Chiama la funzione
    
    /* Nested? No, but we can have multiple
       /* This won't work as nested comment
       but single level works fine */
    
    return 0;
}
