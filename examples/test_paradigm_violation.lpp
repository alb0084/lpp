#pragma paradigm functional

// This should FAIL - violates functional paradigm rules

fn bad_mutable() -> int {
    let mut x = 10;  // ERROR: mutable variable
    x = 20;          // ERROR: reassignment
    return x;
}

fn main() -> int {
    return bad_mutable();
}
